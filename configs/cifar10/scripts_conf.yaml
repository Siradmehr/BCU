# Dataset preparation configuration
num_clients: 10
alpha: 0.5  # Dirichlet concentration (lower = more heterogeneous)
num_classes: 10
dataset_name: "cifar10"
data_root: "./data"
save_path: "./data/cifar10_fl"
seed: 42
